name: keystroke_listener
description: A Flutter package for keystroke_listener functionality
issue_tracker: https://github.com/SoundSliced/keystroke_listener/issues
repository: https://github.com/SoundSliced/keystroke_listener
version: 2.1.1
homepage: https://github.com/SoundSliced/keystroke_listener

environment:
  sdk: '>=3.0.0 <4.0.0'
  flutter: ">=3.0.0"

dependencies:
  flutter:
    sdk: flutter 
  s_packages: ^1.4.1 

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true

